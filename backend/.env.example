# Copy to .env and fill in your values

JWT_SECRET=change-me-to-a-random-secret-string

# SMTP config (optional — if not set, OTP codes are logged to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@mortgage.abdlh.com

PORT=3010
